import React from "react";
import SectionPanel from "../../../common/SectionPanel";

const MainContent = () => <div>main content</div>;

const Devices = () => (
  <SectionPanel title="Devices">
    <MainContent />
  </SectionPanel>
);

export default Devices;
